apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: task-management
  namespace: task-management
spec:
  interval: 5m
  releaseName: task-management

  chart:
    spec:
      chart: .
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m

  valuesFrom:
    - kind: ConfigMap
      name: task-management-values
      valuesKey: values.yaml

  install:
    remediation:
      retries: 3

  upgrade:
    remediation:
      retries: 3