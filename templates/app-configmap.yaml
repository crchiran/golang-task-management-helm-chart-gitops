apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "task-management.appConfigName" . }}
  namespace: {{ include "task-management.namespace" . }}
  labels:
    {{- include "task-management.appLabels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.app.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}